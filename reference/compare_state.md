# Compare two directory states.

Compare two directory states.

## Usage

``` r
compare_state(old, new)
```

## Arguments

- old:

  previous state

- new:

  current state

## Value

list containing number of changes and files which have been `added`,
`deleted` and `modified`

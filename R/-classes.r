if (!exists("Suite")) {
  Suite <- Object$clone()
    SuiteCase <- Suite$clone()
    ChattySuite <- Suite$clone()
    SummarySuite <- Suite$clone()
    StopSuite <- Suite$clone()  
    MinimalSuite <- Suite$clone()  
}